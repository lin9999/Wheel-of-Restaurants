{
  "name": "wheel-or-restaurants",
  "version": "0.1.0",
  "private": true,
  "scripts": {
  	"install": "cd backend && yarn install",
    "start": "cd backend && babel-node server.js",
    "server": "cd backend && yarn server"
  }
}
